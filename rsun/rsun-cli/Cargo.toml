[package]
name = "rsun-cli"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "rsun"
path = "src/main.rs"

[dependencies]
rsun-core = { path = "../rsun-core", features = ["io"] }
clap = { version = "4", features = ["derive"] }
